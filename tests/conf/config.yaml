defaults:
  - _self_
  - providers/huggingface
  - providers/kaggle
  - providers/roboflow
  - species/species_diversity

# Base configuration settings
base_dirs:
  cache: "../testing/cache"
  weights: "../testing/weights"
  datasets: "../testing/datasets"

# Allow for dynamic overrides
hydra:
  run:
    dir: .
  output_subdir: null
